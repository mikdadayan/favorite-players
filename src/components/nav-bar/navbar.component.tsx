import React, { FC } from "react";
import "./navbar.css";

const Navbar: FC = () => {
  return (
    <nav className="navbar">
      <span>Favorite NBA Players</span>
    </nav>
  );
};

export default Navbar;
